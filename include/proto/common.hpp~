#pragma once

namespace proto {

extern float const SHIP_STEER_SPEED;
extern float const SHIP_MOVE_SPEED;
extern float const SHIP_SHOOT_DELAY;
extern float const CANNONBALL_MOVE_SPEED;
extern float const CANNONBALL_SLOWDOWN_SPEED;
extern float const CANNONBALL_MIN_SPEED;
extern float const CANNONBALL_RISE_SCALE;
extern float const CANNONBALL_FALL_SCALE;
extern float const CANNONBALL_RISE_RATIO;

} // ::proto

