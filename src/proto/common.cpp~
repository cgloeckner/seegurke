#include <proto/common.hpp>

namespace proto {

float const SHIP_STEER_SPEED = 80.f;
float const SHIP_MOVE_SPEED = 80.f;
float const SHIP_SHOOT_DELAY = 0.5f;
float const CANNONBALL_MOVE_SPEED = 250.f;
float const CANNONBALL_SLOWDOWN_SPEED = 0.92f;
float const CANNONBALL_MIN_DISTANCE = 125.f;
float const CANNONBALL_MIN_SPEED = 0.2f;
float const CANNONBALL_RISE_SCALE = 1.025f;
float const CANNONBALL_FALL_SCALE = 0.975f;
float const CANNONBALL_RISE_RATIO = 0.9f;

} // ::proto

